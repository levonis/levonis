سأصلح المشكلة من مسارين: جودة الاستخراج نفسها، وصلاحيات/حفظ المساعد `hussain20041010@gmail.com` بدون كشف التكلفة والربح.

## التشخيص الذي ظهر من الفحص
- استخراج الرابط يستدعي `extract-product-info`، وهو يسمح حالياً بدورَي admin و assistant.
- الواجهة تملأ بعض الحقول عبر عناصر النموذج في DOM، لذلك إذا لم يكن النموذج جاهزاً قد لا تُحفظ الأبعاد/الوصف رغم أن الاستخراج رجع بها.
- عند إعادة الاستخراج، الصور الحالية قد تُصفّر إذا لم ترجع صور جديدة، وهذا يسبب فقدان الصور عند الحفظ.
- حفظ الخيارات يعتمد على RPC، لكن دالة مزامنة الخيارات تستخدم `has_admin_access` وقد تحتاج تثبيت صريح لدور assistant.
- المساعد يحتاج صلاحية إدارة المنتجات مثل الأدمن، مع استمرار إخفاء حقول التكلفة والربح عنه في القراءة والواجهة.

## خطة الإصلاح
1. **تثبيت صلاحية المستخدم كمساعد**
   - إضافة/تأكيد دور `assistant` للمستخدم صاحب البريد `hussain20041010@gmail.com` عبر migration آمنة تعتمد على جدول الأدوار.
   - عدم منحه دور admin.

2. **إصلاح حفظ المنتج للمساعد**
   - تحديث دوال حفظ المنتجات/الخيارات لتقبل `admin` و `assistant` بوضوح.
   - منع المساعد من قراءة حقول التكلفة والربح عبر views/column privileges، مع السماح للحفظ بالحقول التشغيلية المطلوبة للمنتج.
   - جعل مزامنة الخيارات تعمل للمساعد، مع عدم إجبار إدخال cost fields من الواجهة.

3. **إصلاح تعبئة نتائج الاستخراج في واجهة الإدارة**
   - جعل الحقول المستخرجة تُحفظ في state/backup snapshot وليس DOM فقط، حتى لا تضيع الأبعاد والوصف إذا تأخر تحميل النموذج.
   - عند الحفظ، استخدام القيم المستخرجة كاحتياط إذا لم توجد في FormData.
   - عدم مسح الصور القديمة أثناء إعادة الاستخراج إلا إذا رجعت صور جديدة صالحة؛ ودمج الصور الجديدة بدون تكرار.

4. **تحسين الاستخراج جذرياً**
   - إبقاء Gemini Pro للاستخراج، لكن فصل الاستخراج إلى مراحل أكثر موثوقية: بيانات عامة، صور، ألوان/خيارات/SKU، أبعاد/وزن.
   - تحسين JSON parsing لكل رد AI، مع كشف truncation/finish_reason وإعادة المحاولة للجزء الناقص بدلاً من قبول نتيجة ناقصة.
   - دمج المصادر بالترتيب: JSON-LD/meta/Shopify API/Bambu parser/Taobao SKU regex/AI، ثم تنظيف النتائج.

5. **إصلاح الصور والألوان والخيارات**
   - استخراج كل صور المعرض وعدم قصها مبكراً قبل الدمج والتنظيف.
   - الاحتفاظ بصور الألوان/الخيارات داخل عناصرها وعدم استبدالها بصورة المنتج الرئيسية.
   - توحيد أسماء المتغيرات/SKU وإزالة التكرار مع حفظ `taobao_sku_id` عند توفره.

6. **إصلاح الأبعاد والوصف**
   - إضافة fallback مباشر من meta/JSON-LD/النص المرئي للأوصاف إذا رجع AI وصفاً قصيراً أو فارغاً.
   - تحسين أولوية أبعاد التغليف والوزن الإجمالي، مع عدم استبدال قيم موجودة بقيم فارغة.

7. **التحقق بعد التنفيذ**
   - اختبار استدعاء `extract-product-info` فعلياً برابط منتج فيه صور/ألوان/خيارات.
   - اختبار حفظ منتج جديد وتعديل منتج من جلسة مساعد قدر الإمكان، أو التحقق من RPC/السياسات إذا لم تتوفر جلسة مصادقة.
   - التأكد أن المساعد يرى المنتجات ويديرها، ولا يرى حقول التكلفة والربح.