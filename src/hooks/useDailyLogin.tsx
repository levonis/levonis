// Daily login hook - disabled (tasks are now manual via DailyTasksPanel)
// Kept as empty hook for backward compatibility
export function useDailyLogin() {
  // No-op: daily login is now a manual task
}
